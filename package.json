{
  "name": "castle-packaging-website",
  "private": true,
  "description": "Castle Packaging website — static site, no build step. No npm dependencies needed: the Product Chatbot Function (netlify/functions/chat.js) only reads local JSON files and calls the Anthropic API via fetch. The old Social Media auto-share feature that used to need @netlify/blobs here has been retired in favor of the Social Media Campaign Manager in the Business Suite app.",
  "dependencies": {}
}
